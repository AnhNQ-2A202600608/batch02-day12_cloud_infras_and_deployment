# Screenshots Folder

Vui lòng đặt các ảnh chụp màn hình sau vào thư mục này trước khi nộp bài:
1. `dashboard.png`: Giao diện Dashboard (Railway, Render, GCP) hiển thị dịch vụ đã deploy thành công cùng với cơ sở dữ liệu Redis đi kèm.
2. `running.png`: Dòng log chạy của service trong trang quản lý Cloud hiển thị lúc khởi chạy thành công.
3. `test.png`: Ảnh chụp màn hình chạy test API bằng Postman, curl hoặc một client khác hiển thị thành công (HTTP 200) và lỗi khi thiếu key (HTTP 401) hoặc vượt limit (HTTP 429).
